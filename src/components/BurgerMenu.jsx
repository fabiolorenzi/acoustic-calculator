import React, { useState } from "react";
import "./BurgerMenu.style.css";

function BurgerMenu() {
    const [open, setopen] = useState(false);

    return(
        <div className="burgerMenu"><h1>hello</h1></div>
    );
};

export default BurgerMenu;